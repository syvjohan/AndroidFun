<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chat_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.johan.firebase.ChatActivity$PlaceholderFragment">

    <LinearLayout
        android:id="@+id/linear_top"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/linear_bottom"
        android:layout_marginBottom="30dp"
        android:orientation="horizontal">

        <ListView
            android:id="@+id/listView_chat_message_me"
            android:layout_width="0dp"
            android:layout_weight=".50"
            android:layout_margin="2dp"
            android:layout_height="wrap_content" />

        <ListView
            android:id="@+id/listView_chat_others"
            android:layout_width="0dp"
            android:layout_weight=".50"
            android:layout_margin="2dp"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/linear_bottom"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true">

        <EditText
            android:id="@+id/txt_message_input"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:hint = "write text here"
            android:textSize="15sp"
            android:background="#F8F8F8"/>
        </LinearLayout>

</RelativeLayout>
